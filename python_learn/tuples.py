#tuples are similar to lists but with IMMUTABILITY
#cannot be changed. data integrity
t = (1,2,3,1)
print(t)
print(len(t))
#can mix object types
#built in method: how many(count) and the index(index)
print(t.count(1))
print(t.index(1))